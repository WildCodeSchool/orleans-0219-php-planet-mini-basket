<section class="news container">
    <h2 class="mb-1 text-center" >Nos Actualités</h2>
        <div class="row">
            <div class="col-md-12">
                <div class="carousel slide" data-ride="carousel" id="quote-carousel">
                    <ol class="carousel-indicators">
                        {% for newsContent in news %}
                        <li class="carousel-news" data-target="#quote-carousel" data-slide-to="{{ loop.index0 }}"{{ loop.first ? ' classe="active"' }}></li>
                        {% endfor %}
                    </ol>
                    <div class="carousel-inner">
                        {% for newsContent in news %}
                        <div class="carousel-item {{ loop.first ? 'active' }}">
                                    <h3 class="text-center mt-2">{{ newsContent.news_title }}</h3>
                                    <p class="text-center">{{ newsContent.news_content }}</p>
                        </div>
                        {% endfor %}
                    </div>
                    <a class="carousel-control-prev" href="#quote-carousel" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon color-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#quote-carousel" role="button" data-slide="next">
                        <span class="carousel-control-next-icon color-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
        </div>
</section>
